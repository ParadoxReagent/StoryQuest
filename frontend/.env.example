# StoryQuest Frontend Environment Variables
# Copy this file to .env for local development

# Backend API URL (default: http://localhost:8000)
VITE_API_URL=http://localhost:8000

# TTS Service URL (default: http://localhost:8001)
# Points to the TTS Docker service for story narration
# Default: Kokoro TTS (fast, works on CPU/Apple Silicon)
# Alternative: Chatterbox TTS (higher quality, requires NVIDIA GPU)
VITE_TTS_URL=http://localhost:8001
